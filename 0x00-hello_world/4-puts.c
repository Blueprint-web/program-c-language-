#include <stdio.h>

/* this is the main */
int main(void)
{
	/* this is the string */
	char str[] = "\"Programming is like building a multilingual puzzle";
	puts(str);
	fprintf(stderr,"");

	/* after printing retun */
	return(0);
}
