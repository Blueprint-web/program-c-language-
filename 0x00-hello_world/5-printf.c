#include <stdio.h>

/**
 * main - for holding the code
 * Return: return no value
 */
int main(void)
{
	/**
	 * print - to print out the function
	 */
	printf("with proper grammar, but the outcome is a piece of art,\n");

	stderr;

	return (0);
}
